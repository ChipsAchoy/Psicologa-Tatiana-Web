import React, { Component } from "react";
import Fade from "react-reveal";

class Mapa extends Component {


  render() {
    return(
      <Fade duration={1000}>
        <section id="mapa">
        mI CARA CUANDO
        </section>
        
      </Fade>
    );
  }
}

export default Mapa;
